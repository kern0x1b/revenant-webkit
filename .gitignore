# Upstream checkouts. Fetched by fetch-source.sh; the port's own changes to the
# engine live in patches/engine/, generated by tools/export-engine-patches.sh.
webkit-254/
webkit-603/
webkit-trunk/
webkit-trunk-jit/

# Dependencies built from source by the build-*.sh scripts.
third_party/

# Build trees. Every one of these is reproducible from configure-*.sh plus
# build-*.sh, and together they are several gigabytes.
build/
build-254*/
build-cocoa/
build-icu-armv7/
build-icu-host/
build-jit/
build-jsc/
build-libcxx/
build-trunk/

# Products: the staged application, the packaged web apps, and object files.
dist/
out/
*.log
*.o
*.a
*.dylib
*.tar
*.tar.gz

.DS_Store
device.env
